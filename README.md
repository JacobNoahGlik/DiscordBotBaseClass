# DiscordBotBaseClass
Python class built to make creating a Discord bot in python more object oriented instead of script style

## Features
* Ability to check if the bot was @mentioned
* Ability to easily add `on message`, `send`, and `reply` functions, without needing to understand the complicated `discord.py` library
